Refactorizando y usando variables de loading y error

-En esta clase vemos algunas buenas practicas:
    .Manejar y renderizar diferentes cosas dependiendo si se esta en loading o en error.
    .La query (lo que sea gql`query`), debe estar dentro de una constante, para usarla cuando se necesite.