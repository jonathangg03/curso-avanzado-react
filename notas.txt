Instalación de React y Babel

-Sí no queremos usar un archivo aparte para babel, porque solo tenemos los 2 plugins, podemos agregar en las
    options del loader de babel un array presets, con ambos presets instalados (env y react).