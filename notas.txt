SVGR: de SVG a componente de ReactJS

-Crearemos el logo desde 0, usando la app maketext.io
-Colocamos un texto, seleccionamos el que nos guste, y descargamos como svg.
-Dicho texto, lo vamos a pasar a al optimizador de SVG  https://jakearchibald.github.io/svgomg/, simplemente
    abriendolo o arrastrando el archivo hacia el centro de la página.
        .Tenemos 2 opciones, guardarlo para usarlo como imagen, esta no la hicimos.
        .La otra es copiar el svg generado, y pasarlo a una página que nos convierta el svg que ingresemos en
            un componente de React.
        .Esa página es https://react-svgr.com/playground/, y solamente pegamos el contenido en la derecha, y a
            la izq. nos dá el componente de react, que pegaremos en un js.
        .A este le podemos dar estilos creando su archivo style igualmente, donde la etiqueta usada sea svg.