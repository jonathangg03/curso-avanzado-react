Usar información real de las categorías

-Cuando vamos a usar props, sí queremos usar todas las props en lugar de hacer <Component atr1={val}...>, lo
    que hacemos es esto <Component {...object}>, sí object tiene keys con el mismo nombre de las props, le dara
    los valores de esas key a las props, y se coloca solo una vez.